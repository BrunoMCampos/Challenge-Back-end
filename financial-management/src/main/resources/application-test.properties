spring.jpa.hibernate.ddl-auto=create
spring.datasource.url=${FINANCIALMANAGEMENT_DATABASE_URL}
spring.datasource.username=${FINANCIALMANAGEMENT_DATABASE_USERNAME}
spring.datasource.password=${FINANCIALMANAGEMENT_DATABASE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
financialmanagement.jwt.expiration=18000000
financialmanagement.jwt.secret=${FINANCIALMANAGEMENT_JWT_SECRET}